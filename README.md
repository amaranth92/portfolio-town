# Timeline Platformer Portfolio

Portrait-first side-view pixel platformer portfolio built with React, TypeScript, Vite and Phaser.

## Local

```bash
npm install
npm run dev
```

## Checks

```bash
npm run typecheck
npm run lint
npm run build
```

## Deploy

Build output can be pushed to a `gh-pages` branch and served from GitHub Pages.
